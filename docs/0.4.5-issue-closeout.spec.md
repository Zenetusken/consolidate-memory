# 0.4.5 issue-closeout arc — spec (design-of-record)

**STATUS (2026-09-03): proposed v2 — advisor-amended, awaiting approval.** The arc
that closes the remaining open GitHub issues, following the 2026-09-03 audit of all
25 issue-board findings against HEAD `c0eaed1` (v0.4.4), plus an advisor pass whose
findings (D1–D3, R1–R3, N1–N5) are folded in below.

## Context: what the audit found

The board's 25 issues were the preflight/PR-#128 audit findings, filed
2026-09-02 from a pre-0.3.7/v0.4.0 tree. The code has outrun the board:
**22 of 25 were already fixed by shipped commits** — verified against live
HEAD by five code-level audit agents (2026-09-03) and closed with
commit-and-evidence comments:

| Cluster | Issues | Fixing commit(s) |
|---|---|---|
| Phase-3 domain/canonical lifecycle | #137, #140, #141, #147 | `f696892` |
| Phase-2 journal/DB core | #138, #139, #143, #144, #148 | `1b5221e` + `fbdde11` + `2bacd0b` |
| Phase-4 sole-authority | #142, #145, #146, #149 | `7f0483e` + `df18912` |
| Phase-1 primitives (R128-1…8) | #129–#136 | `3f9c93a` + `b504b6a` + `87922a1` |
| Concurrency suite | #150 | `3f9c93a` + `7204db3` — 14 of 15 scenarios; the 15th (compaction after high baseline) is genuinely absent and is PR #180's scope (the close comment was amended to say so) |

**Three issues remain open**, each with a small, precise delta — the arc's work.
There are no fully-open issues and no unknown-scale fixes left.

## Closure mechanics (explicit — GitHub has no "code leg of" keyword)

- **#151** closes when PR #180 merges (its last acceptance item lands then).
- **#152** closes **only after** PR #181 merges AND the ruleset operator action
  lands AND its AGENTS.md required-check documentation ships (PR #183). The PR
  body uses no auto-close keyword — the issue stays the open tracker for the
  operator leg.
- **#153** closes after PR #182 merges AND the operator-evidence tracking issues
  are filed. The arc's "`gh issue list --state open` = zero" check therefore
  only passes after the operator legs.

## The PRs, in merge order

The PRs touch near-disjoint file sets; the only adjacency is `tests/smoke.py`
(#179 vs #181, different regions). Merge oldest-first, each independently green.

### PR #178 — the spec (this document)

- **Files:** `docs/0.4.5-issue-closeout.spec.md`.
- **Gate:** doc-only. Adversarial spec review per the standing workflow, then merge.

### PR #179 — L2 renderer hotfix: distill top-commands move from the header into the body

Already implemented in the working tree (2026-09-03 hotfix, user-reported
defect): the HTML archive's "recurring workflows" header carried the top-3
command list on its counts line and wrapped across rows. The list now lives in
the section body (`dstl-top` / `dstl-row`), modeled on the registrar board's
idiom with its own layout classes; the counts line carries counts only. The
smoke pin holds both directions (body list present AND header appendix absent).

- **Files:** `plugins/consolidate-memory/scripts/dashboard.template.html`,
  `tests/smoke.py` (1 pin replaced by 1 — net zero; the pass count is re-read
  from the actual run at merge, never assumed).
- **Gate:** smoke / sim / manifests / mypy. Plugin-artifact change — this is
  what makes 0.4.5 a release rather than repo-only churn.
- **Issue:** none (dogfood defect, fixed before filing).

### PR #180 — CI bench corner + the 15th concurrency scenario + the CI-jobs doc fix

- **`#151` leg — the bench corner:** add a `bench` job to
  `.github/workflows/ci.yml` (ubuntu-latest, python 3.12,
  `timeout-minutes: 10`) running `python3 tests/bench_phase5.py --quick`,
  plus a step storing the `--json` report as a run artifact (the "large
  corners documented" acceptance item gets its home: the artifact + one
  sentence in the PR body; `--quick` exits 0 by design — measured, not
  gated).
- **`#150` leg — the missing 15th scenario:** add the compaction-vs-concurrent-
  writer barrier test to `tests/concurrency.py` (compact running under a high
  row baseline while a writer transacts; assert filesystem + registry +
  journal + return-code postconditions) — completing the issue's "Full 15
  scenarios" acceptance.
- **`AGENTS.md` doc fix (advisor D2):** the CI paragraph claims "one workflow,
  **4** jobs" and omits `concurrency` (today, 5 jobs; 6 after this PR) —
  correct the count and name all jobs.
- **Files:** `.github/workflows/ci.yml`, `tests/concurrency.py`,
  `AGENTS.md` (CI paragraph only).
- **Gate:** the new jobs green on the PR.
- **Closes:** #151. **Completes:** #150's remaining acceptance.

### PR #181 — SHA256SUMS in the release pipeline (#152's code leg)

Signed tags, the committed-pubkey verify job, SLSA provenance, and the SPDX
SBOM all shipped (v0.4.2 R5); the release assets still lack a plain
`SHA256SUMS`. Add a stdlib generator + the workflow step; the 0.4.5 release
self-tests the new step.

- **Files:** `tools/make_checksums.py` (stdlib; sha256sum-formatted inventory,
  manifest paths **repo-root-relative**, same walk as `tools/make_sbom.py` —
  sorted, symlinks/non-files skipped — so `sha256sum -c` resolves from the
  repo root), `.github/workflows/release.yml` (provenance job: generate +
  upload `SHA256SUMS` beside the SBOM — the job already has `contents:
  write`), `tests/smoke.py` (pin: generator output verifies under
  `sha256sum -c` for a fixture tree — the issue's "dry-run produces
  checksums" test, zero-dep).
- **Gate:** smoke / sim / manifests / **mypy** (smoke.py is in mypy scope) +
  the maintainer dry-run (`make_checksums.py` → `sha256sum -c` from the repo
  root).
- **Closes:** #152's code leg (issue stays open — see closure mechanics).

### PR #182 — POSIX-only declaration (#153's doc leg)

`docs/1.0-preflight.spec.md`'s STATUS ops-HOLD sentence still lists "native
Windows mutation" as a blocker, but ADR 016 already chose POSIX/WSL as the
mutation contract. Declare 1.0 POSIX-only; the Windows item becomes a stated
non-goal (citing ADR 016), not a blocker. The soak/clean-machine items remain
HOLD — operator evidence, not code.

- **Files:** `docs/1.0-preflight.spec.md` — the STATUS paragraph's ops-HOLD
  sentence (the Windows text lives THERE, not in the Residuals table — N1).
- **Gate:** smoke (genericity scan) + doc-only.
- **Closes:** #153's doc leg (issue stays open — see closure mechanics).

### PR #183 — release prep (advisor D1 — the arc's executable release)

No earlier PR carries the release mechanics; this one does:

- **Files:** `CHANGELOG.md` (`## [0.4.5]` section, authored first — the single
  source of truth), `plugins/consolidate-memory/.claude-plugin/plugin.json`
  (0.4.4 → 0.4.5), the **version-statement sweep to 0.4.5 — targeting the
  shipping version** (the 0.4.4 lesson): CLAUDE.md/AGENTS.md headers + the
  AGENTS.md plugin-table row, SKILL.md banner, harness-map.md, README's four
  current-release statements, the preflight STATUS version line (ordered
  AFTER #182, which edits the same paragraph — N2). Historical feature-origin
  attributions untouched. Also the AGENTS.md Releasing bullet documenting the
  required-check list (the #152 doc leg).
- **Gate:** smoke / sim / manifests / mypy + `./release.sh --expect patch
  --dry-run` classifies already-bumped (patch) — the 0.4.4 commit shape.
- **Then:** the user merges → `./release.sh --expect patch --finalize` —
  v0.4.5 becomes the first release attaching SBOM **and** SHA256SUMS (the
  merged release.yml is what runs on the v0.4.5 tag), and the ruleset
  operator action should land before finalize so the release exercises the
  full governance.

## Operator actions (not PRs — the user's)

1. **Branch ruleset** (#152's remaining leg): on `protect-main` set
   `required_approving_review_count >= 1` and required status checks. The
   ruleset matches check-run **display names, never job keys** — the exact
   list: `test (python 3.8)`…`test (python 3.13)`, `test (macos python 3.12)`,
   `concurrency (linux python 3.12)`, `typecheck (mypy, dev-only contract
   check)`, `plugin manifest validation (claude CLI)`, `bench (linux python
   3.12)` (after #180). Documented in AGENTS.md by #183.
2. **1.0 qualification evidence** (#153's remaining leg): fleet soak of a
   copy of a pre-0.3 fleet, clean-machine install walkthrough, second-profile
   multi-user validation, Auto Dream coexistence re-verify — filed as
   operator tracking issues; the GO call remains the user's.

## Release plan

**0.4.5 patch** via the two-phase flow. Versioning: all changes are
backward-compatible (one cosmetic renderer fix + CI/docs) → **patch** per the
policy. The release exercises PR #181 on itself.

## Verification for the arc

- Per-PR: smoke / sim / manifests / mypy + a review agent per PR (the standing
  multi-PR-chain rule).
- Arc close: `gh issue list --state open` = zero — reached only after the
  operator legs above; the 0.4.5 release carries SBOM + SHA256SUMS with green
  verify + provenance jobs.
